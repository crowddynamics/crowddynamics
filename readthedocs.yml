# https://docs.readthedocs.io/en/latest/yaml-config.html

formats:
    - none

conda:
    file: environment.yml

python:
  version: "3.6"
  pip_install: true
  extra_requirements:
        - tests
        - docs
