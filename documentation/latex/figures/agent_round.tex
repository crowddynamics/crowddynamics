\begin{tikzpicture}
  [
    point/.style = {draw, circle, fill=white, inner sep=1.4pt}
  ]

\input{./grid}


\def \radius {5};
\def \angle {-30};

% Centre
\coordinate[] (centre) at (0, 0);
\node[below=3pt] (agent) at (centre) {$ \mathbf{x}_{i} $};

% Circle
\draw[gray] (centre) circle (\radius);

% Ellipse
\draw[] (centre) ellipse (5 and 3);

% Axis
\draw[dashed, ->] (centre) -- node[above] {$ a $} ($ (centre) + (5, 0) $);
\draw[dashed, ->
] (centre) -- node[right] {$ b $} ($ (centre) + (0, 3) $);

%\draw[<->] (centre) -- node[above] {$ r_{i} $}
%           ($ (centre) + ({\radius * cos(\angle)}, {\radius * sin(\angle)} )$);


\end{tikzpicture}